/*
    This file contains the data structures that are used to represent the state of the application.
    States are only data without any functioanlity. 
    StateManager is a class that holds a pointer to all the states registered to it and manages the lifetime of the states.

*/




//---------------------------------- [Forward declarations] ----------------------------------//
struct StateManager;

struct WindowState;
struct MainMenuBarState;
